#include <stdio.h>
#include <ctype.h>

/**
 * (3pts)
 * Create a loop that reads characters from stdin and prints the input but w/o
 * any of the vowels (a,e,i,o and u) be they uppercase or lowercase.
 */

int main(int argc, char *arv[])
{


  return 0;
}
